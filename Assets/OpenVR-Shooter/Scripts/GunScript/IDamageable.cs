using UnityEngine;

// Inheritance Class 
// No matter what the actual class content is, you must unconditionally create the OnDamage Function


public interface IDamageable
{
	void OnDamage(float damage);
}